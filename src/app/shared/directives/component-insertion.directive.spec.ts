import { ComponentInsertionDirective } from './component-insertion.directive';

describe('ModalInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new ComponentInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
