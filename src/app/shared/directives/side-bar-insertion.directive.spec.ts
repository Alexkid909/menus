import { SideBarInsertionDirective } from './side-bar-insertion.directive';

describe('SideBarInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new SideBarInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
